package ch.frankel.blog.renamer;

/**
Dummy class.

<p>Because the Java compiler runs before the Kotlin compiler,
 at least a single Java class is necessary inside this package
 so that the Java compiler can correctly compile <code>module-info.java</code>.</p>

<p>Without this file, the compiler fails with the failure:</p>

<pre>package is empty or does not exist: ch.frankel.blog.renamer</pre>
 */
@Deprecated
public class ForModuleInfoToCompile {}
